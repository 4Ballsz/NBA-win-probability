# Minimum versions rather than exact pins: the original pins (numpy 1.24.3,
# pandas 2.0.3) have no wheels for Python 3.12+ and fail to install there.
numpy>=1.24
pandas>=2.0
xgboost>=2.0
scikit-learn>=1.3
nba_api>=1.4
